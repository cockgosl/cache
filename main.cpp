#include <iostream>
#include <unordered_map>
#include <list>
using namespace std;

size_t cash_func(size_t s, istream& is) {
    size_t hits = 0;
    list<int> pages;
    unordered_map<int, list<int>::iterator> table;


    return hits;
}

int main() {
    return 0;
}
